(appapi) {
	@public path /api/auth/* /api/billing/* /api/stripe/* /api/waitlist /api/profile /api/bot /api/bot/connect /api/bot/messages /api/tg/* /api/concierge /admin /admin/* /api/status /api/agent /chat /api/oracle /api/atlas /api/send /api/approve /api/brief /api/inbox /api/pixel /three.min.js /jarvis-startup.mp3
	handle @public {
		header {
			Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
			X-Content-Type-Options "nosniff"
			X-Frame-Options "DENY"
			Referrer-Policy "strict-origin-when-cross-origin"
		}
		reverse_proxy 127.0.0.1:7700 {
			header_up X-Real-IP {remote_host}
		}
	}
	handle {
		respond "404" 404
	}
}

api.noxioai.com {
	import appapi
}

95.179.242.172.nip.io {
	import appapi
}
